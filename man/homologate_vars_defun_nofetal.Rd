% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/homologate_vars_defun_nofetal.R
\name{homologate_vars_defun_nofetal}
\alias{homologate_vars_defun_nofetal}
\title{Homologate variables of interest for Defunciones No Fetales}
\usage{
homologate_vars_defun_nofetal(
  defun_nofetal_raw,
  ddi_defun_nofetal_vars,
  ddi_nac_vars
)
}
\arguments{
\item{defun_nofetal_raw}{a data frame with defunciones no fetales data,
as returned by \code{ingest_eevv_defun_nofetal}}

\item{ddi_defun_nofetal_vars}{DDI metadata for defunciones no fetales, as
returned by \code{ingest_eevv_ddi("nofetal")}}

\item{ddi_nac_vars}{A data frame containing information about variables
from Nacimientos in DDI files as returned by \code{ingest_eevv_ddi()}. Used
as a fallback for variables whose categories are, for whatever reason,
undocumented in the Defunciones DDI (currently just \code{idclasadmi}) but
documented in the Nacimientos one for the same underlying EEVV coding
system.}
}
\value{
a data frame with a similar structure as \code{defun_nofetal_raw}
}
\description{
Defunciones No Fetales carries every variable \code{homologate_vars_defun()}
already handles, plus a handful specific to this dataset (deceased's own
demographics -- since the deceased is not a fetus/newborn here -- and a
few pregnancy-context flags). This function applies the common
homologation first and then handles those extra variables. See
\code{homologate_vars_defun()} for the shared logic and its caveats.
}
\seealso{
\code{homologate_vars_defun}, \code{homologate_vars_defun_fetal}
}
