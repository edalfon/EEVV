% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_nac_vars.R
\name{select_nac_vars}
\alias{select_nac_vars}
\title{Select final Nacimientos variables and flatten the nested data}
\usage{
select_nac_vars(nac_homo, ddi_nac_var_labels)
}
\arguments{
\item{nac_homo}{a data frame with homologated nacimientos data, including
a \code{sav_data} column with nested data frames, as returned by
\code{homologate_vars}}

\item{ddi_nac_var_labels}{a named character vector of variable labels,
keyed by variable name, as returned by \code{get_var_labels_only}}
}
\value{
a flat (not nested) data frame with the selected variables,
each one carrying its corresponding label (as the \code{"label"} attribute)
}
\description{
Takes the homologated Nacimientos data (still nested by year in the
\code{sav_data} column) and keeps only the variables of interest for
downstream analysis. The nested data frames are then unnested into a
single flat data frame and, since \code{tidyr::unnest()} drops variable
labels, the labels are re-attached from \code{ddi_nac_var_labels}.
}
\seealso{
\code{homologate_vars}, \code{get_var_labels_only}
}
