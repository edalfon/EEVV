% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_defun_nofetal_vars.R
\name{select_defun_nofetal_vars}
\alias{select_defun_nofetal_vars}
\title{Select final Defunciones No Fetales variables and flatten the nested data}
\usage{
select_defun_nofetal_vars(defun_nofetal_homo, ddi_defun_nofetal_var_labels)
}
\arguments{
\item{defun_nofetal_homo}{a data frame with homologated defunciones no
fetales data, as returned by \code{homologate_vars_defun_nofetal}}

\item{ddi_defun_nofetal_var_labels}{a named character vector of variable
labels, keyed by variable name, as returned by \code{get_var_labels_only}}
}
\value{
a flat (not nested) data frame with the selected variables,
each one carrying its corresponding label (as the \code{"label"} attribute)
}
\description{
Mirrors \code{select_nac_vars()}/\code{select_defun_fetal_vars()}: takes the
homologated Defunciones No Fetales data (still nested by year in the
\code{sav_data} column), keeps the variables handled by
\code{homologate_vars_defun()}/\code{homologate_vars_defun_nofetal()}, unnests
into a single flat data frame, and re-attaches variable labels (dropped
by \code{tidyr::unnest()}). Most variables are wrapped in \code{any_of()} rather
than referenced bare, since several only exist in a subset of years.
}
\seealso{
\code{select_nac_vars}, \code{select_defun_fetal_vars},
\code{homologate_vars_defun_nofetal}
}
