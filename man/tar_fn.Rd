% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overrides.R
\name{tar_fn}
\alias{tar_fn}
\title{Run each top-level statement of a function's body via \code{eval()}}
\usage{
tar_fn(fn)
}
\arguments{
\item{fn}{a function whose body is a \code{{ ... }} block}
}
\value{
a list with the value of each top-level statement in \code{fn}'s body
}
\description{
Takes a function \code{fn} whose body is a \code{{ ... }} block, and evaluates each
of its top-level statements in turn via \code{eval()}, inside \code{tar_fn()}'s own
call frame -- not inside a call frame for \code{fn} itself, and not inside the
environment that called \code{tar_fn()} either. In practice this means: \code{fn}'s
formal arguments are never bound (references to them inside the body
will error), and any variables the body assigns via \verb{<-} are local to
this call and discarded once \code{tar_fn()} returns -- they do not leak into
the caller's environment. The return value is a list of every
statement's result (via \code{lapply()}), not just the last one, which is the
main way this differs from simply calling \code{fn()}.
}
\details{
Currently unused anywhere in this package; kept here as-is rather than
guessing at removal, since its intent isn't documented elsewhere.
}
\keyword{internal}
