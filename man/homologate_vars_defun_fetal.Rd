% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/homologate_vars_defun_fetal.R
\name{homologate_vars_defun_fetal}
\alias{homologate_vars_defun_fetal}
\title{Homologate variables of interest for Defunciones Fetales}
\usage{
homologate_vars_defun_fetal(
  defun_fetal_raw,
  ddi_defun_fetal_vars,
  ddi_nac_vars
)
}
\arguments{
\item{defun_fetal_raw}{a data frame with defunciones fetales data, as
returned by \code{ingest_eevv_defun_fetal}}

\item{ddi_defun_fetal_vars}{DDI metadata for defunciones fetales, as
returned by \code{ingest_eevv_ddi("fetal")}}

\item{ddi_nac_vars}{A data frame containing information about variables
from Nacimientos in DDI files as returned by \code{ingest_eevv_ddi()}. Used
as a fallback for variables whose categories are, for whatever reason,
undocumented in the Defunciones DDI (currently just \code{idclasadmi}) but
documented in the Nacimientos one for the same underlying EEVV coding
system.}
}
\value{
a data frame with a similar structure as \code{defun_fetal_raw}
}
\description{
Fetal deaths carry the same set of variables as \code{homologate_vars_defun()}
already handles (no variable is specific to this dataset that isn't also
present in Defunciones No Fetales), so this is a thin wrapper around it.
See \code{homologate_vars_defun()} for the actual per-variable logic and its
caveats (notably, cause-of-death and manner-of-death coding are left as
raw pass-throughs, not homologated).
}
\seealso{
\code{homologate_vars_defun}, \code{homologate_vars_defun_nofetal}
}
