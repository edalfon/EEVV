% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_defun_fetal_vars.R
\name{select_defun_fetal_vars}
\alias{select_defun_fetal_vars}
\title{Select final Defunciones Fetales variables and flatten the nested data}
\usage{
select_defun_fetal_vars(defun_fetal_homo, ddi_defun_fetal_var_labels)
}
\arguments{
\item{defun_fetal_homo}{a data frame with homologated defunciones
fetales data, as returned by \code{homologate_vars_defun_fetal}}

\item{ddi_defun_fetal_var_labels}{a named character vector of variable
labels, keyed by variable name, as returned by \code{get_var_labels_only}}
}
\value{
a flat (not nested) data frame with the selected variables,
each one carrying its corresponding label (as the \code{"label"} attribute)
}
\description{
Mirrors \code{select_nac_vars()}: takes the homologated Defunciones Fetales
data (still nested by year in the \code{sav_data} column), keeps the
variables handled by \code{homologate_vars_defun()}, unnests into a single
flat data frame, and re-attaches variable labels (dropped by
\code{tidyr::unnest()}). Unlike \code{select_nac_vars()}, most variables here are
wrapped in \code{any_of()} rather than referenced bare, because several of
them (e.g. \code{hora}, \code{causa_667}, \code{t_ges_agru_cie}) only exist in a subset
of years -- a bare reference would error on years missing that column.
}
\seealso{
\code{select_nac_vars}, \code{homologate_vars_defun_fetal}
}
