% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/homologate_vars_defun.R
\name{homologate_vars_defun}
\alias{homologate_vars_defun}
\title{Homologate variables shared by both Defunciones datasets}
\usage{
homologate_vars_defun(defun_i, ddi_defun_vars, ddi_nac_vars)
}
\arguments{
\item{defun_i}{a data frame with nacimientos data, including
columns year and sav_data, that contains nested data frame for each year
as returned by \code{ingest_eevv_defun_fetal}/\code{ingest_eevv_defun_nofetal}}

\item{ddi_defun_vars}{A data frame containing information about
variables from the corresponding Defunciones dataset in DDI files, as
returned by \code{ingest_eevv_ddi("fetal")}/\code{ingest_eevv_ddi("nofetal")}}

\item{ddi_nac_vars}{A data frame containing information about variables
from Nacimientos in DDI files as returned by \code{ingest_eevv_ddi()}. Used
as a fallback for variables whose categories are, for whatever reason,
undocumented in the Defunciones DDI (currently just \code{idclasadmi}) but
documented in the Nacimientos one for the same underlying EEVV coding
system.}
}
\value{
a data frame with a similar structure as the input \code{defun_i}
(nested data frames in the \code{sav_data} variable)
}
\description{
Defunciones Fetales and Defunciones No Fetales share the large majority
of their variables (same EEVV death-certificate form), so this function
holds the homologation logic common to both, mirroring the variable-by
-variable approach of \code{homologate_vars()} (for Nacimientos). It is meant
to be called from \code{homologate_vars_defun_fetal()} and
\code{homologate_vars_defun_nofetal()}, not directly.
}
\details{
As with \code{homologate_vars()}, this reflects a first pass grounded in the
DDI codebooks and \code{inspect_vars_info_defun_*.Rmd} /
\code{inspect_ddi_vars_defun_*.Rmd}, not an exhaustive manual review of every
variable the way Nacimientos got (that took a long iterative process).
In particular, the cause-of-death coding (ICD codes: \code{c_bas1}, \verb{c_ant*},
\verb{c_dir*}, \verb{c_pat*}, \code{c_mcm1}, \code{cau_homol}, \code{causa_666}/\code{causa_667},
\code{causa_mult}) and manner-of-death coding (\code{man_muer}/\code{pman_muer} vs. the
post-2019 \code{p_pman_iris}, which use different classification schemes) are
deliberately left as raw/cast-only pass-throughs rather than homologated:
doing that properly needs the actual DANE/OPS/WHO ICD-10 and "Lista
6/67"/"Lista 105"/IRIS documentation to cross-reference codes across
periods, which is a research task on its own.
}
\seealso{
\code{homologate_vars}, \code{homologate_vars_defun_fetal},
\code{homologate_vars_defun_nofetal}
}
